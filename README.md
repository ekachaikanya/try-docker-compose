
Markdown

```
# 🐳 Docker Compose: LEMP Stack Ultimate Guide

ยินดีต้อนรับสู่คู่มือการใช้งาน **Docker Compose** สำหรับจำลอง Web Server ด้วย **LEMP Stack**
โปรเจกต์นี้ออกแบบมาเพื่อการเรียนรู้ โดยเน้นการตั้งค่าที่ใช้งานได้จริง มีความปลอดภัย และจัดการง่าย

### 🏗️ ส่วนประกอบของระบบ (Services)
1.  **Nginx**: Web Server (ด่านหน้าคอยรับ Request)
2.  **PHP-FPM**: PHP Processor (หน่วยประมวลผลโค้ด PHP)
3.  **MariaDB**: Database (ฐานข้อมูลสำหรับเก็บข้อมูล)

---

## 📂 1. โครงสร้างไฟล์ (Project Structure)

ก่อนเริ่มงาน ให้สร้างโฟลเดอร์และไฟล์ตามโครงสร้างนี้:

```text
my-web-project/
├── docker-compose.yml       # 📄 พระเอกของเรา: ไฟล์ควบคุมทุกอย่าง
├── html/
│   └── index.php            # 📄 ไฟล์หน้าเว็บ PHP ทดสอบการเชื่อมต่อ
└── nginx/
    └── default.conf         # 📄 ไฟล์ตั้งค่าให้ Nginx คุยกับ PHP รู้เรื่อง

```

----------

## 🚀 2. การติดตั้งและตั้งค่า (Setup)

### 2.1 สร้างไฟล์ `docker-compose.yml`

ไฟล์นี้กำหนดการทำงานของทั้ง 3 Services, การเชื่อมต่อ Network, และการจัดการข้อมูล (Volume)

YAML

```
version: '3.8'

services:
  # --- 1. Web Server (Nginx) ---
  web:
    image: nginx:latest
    container_name: my-web-server  # ตั้งชื่อ Container เองให้จำง่าย
    restart: always                # ถ้าดับให้ตื่นเองเสมอ (Auto Restart)
    ports:
      - "8080:80"                  # เข้าผ่าน Browser ที่ localhost:8080
    volumes:
      - ./html:/var/www/html       # ผูกโฟลเดอร์โค้ด (Bind Mount)
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro # ผูก Config
    depends_on:
      - php                        # รอให้ PHP รันเสร็จก่อน
    networks:
      - my-web-net                 # ระบุ Network วงเฉพาะ

  # --- 2. PHP Processor (PHP-FPM) ---
  php:
    image: bitnami/php-fpm:8.1     # ใช้ Image สำเร็จรูปที่มี Extension ครบ
    container_name: my-php-fpm
    restart: always
    volumes:
      - ./html:/var/www/html       # ต้องผูกที่เดียวกับ Nginx
    networks:
      - my-web-net

  # --- 3. Database (MariaDB) ---
  db:
    image: mariadb:10.6
    container_name: my-mariadb
    restart: always
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_DATABASE: mydatabase
      MARIADB_USER: user
      MARIADB_PASSWORD: password
    volumes:
      - db_data:/var/lib/mysql     # เก็บข้อมูลถาวร (Named Volume)
    networks:
      - my-web-net

# พื้นที่เก็บข้อมูลถาวรของ Database (ลบ Container ข้อมูลไม่หาย)
volumes:
  db_data:

# สร้าง Network วงใหม่เพื่อความปลอดภัย
networks:
  my-web-net:
    driver: bridge

```

### 2.2 สร้างไฟล์ `nginx/default.conf`

ตั้งค่าให้ Nginx ส่งไฟล์ `.php` ไปหา Container PHP

Nginx

```
server {
    listen 80;
    server_name localhost;
    root /var/www/html;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    # จุดสำคัญ: การส่งค่าไปหา PHP (Reverse Proxy)
    location ~ \.php$ {
        # ส่งไปที่ Service ชื่อ "php" ที่ Port 9000
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }
}

```

### 2.3 สร้างไฟล์ `html/index.php`

ทดสอบโค้ด PHP และการเชื่อมต่อฐานข้อมูล

PHP

```
<?php
// Hostname คือชื่อ Service ใน Docker Compose ("db")
$servername = "db";
$username = "user";
$password = "password";
$dbname = "mydatabase";

// สร้างการเชื่อมต่อ
$conn = new mysqli($servername, $username, $password, $dbname);

// เช็ค Connection
if ($conn->connect_error) {
    die("❌ Connection failed: " . $conn->connect_error);
}
echo "<h1>✅ Hello Docker! Connected to MariaDB successfully.</h1>";
echo "<p>Container Name: " . gethostname() . "</p>";
?>

```

----------

## 🎮 3. วิธีใช้งาน (Usage)

1.  เปิด Terminal ในโฟลเดอร์โปรเจกต์
    
2.  รันคำสั่งเพื่อเริ่มระบบ:
    
    Bash
    
    ```
    docker-compose up -d
    
    ```
    
3.  เปิด Browser ไปที่: 👉 **http://localhost:8080**
    
4.  คุณควรจะเห็นข้อความว่าเชื่อมต่อสำเร็จ
    

----------

## 📚 4. คำอธิบายเชิงลึก (Deep Dive)

### 🔄 A. Restart Policies (นโยบายการรีสตาร์ท)

กำหนดว่า **"ถ้า Container ดับ หรือ Error จะให้ Docker ทำอย่างไร?"**

**ตัวเลือก**

**คำอธิบาย**

**เหมาะสำหรับ**

**`no`** (Default)

**ดับแล้วดับเลย** ไม่ตื่นขึ้นมาใหม่เอง

งานที่ทำครั้งเดียวจบ

**`always`** ⭐

**ตื่นเสมอ** ไม่ว่าจะเกิดอะไรขึ้น (Error หรือ Restart เครื่อง Server)

Web Server / Database

**`unless-stopped`**

**คล้าย always** แต่ถ้าเราสั่ง `docker stop` ด้วยมือเอง มันจะไม่ตื่นขึ้นมาใหม่เมื่อเปิดเครื่อง

Server ทั่วไป

**`on-failure`**

**ตื่นเฉพาะตอนมีปัญหา** (Error Code) เท่านั้น ถ้าปิดดีๆ จะไม่ตื่น

Script สำคัญ

### 🔗 B. Bind Mount vs Named Volume

ในไฟล์นี้เราใช้ทั้ง 2 แบบ:

1.  **Bind Mount (`./html:/var/www/html`)**:
    
    -   เชื่อมต่อไฟล์ในเครื่องเรา กับ Container โดยตรง
        
    -   **ข้อดี:** แก้โค้ดปุ๊บ หน้าเว็บเปลี่ยนปั๊บ เหมาะสำหรับ Code Web
        
2.  **Named Volume (`db_data:/var/lib/mysql`)**:
    
    -   ให้ Docker จัดการที่เก็บข้อมูลให้
        
    -   **ข้อดี:** ปลอดภัยกว่า ข้อมูลไม่หายเมื่อลบ Container เหมาะสำหรับ Database
        

### 🌐 C. Networks (`my-web-net`)

เราสร้าง Network ชื่อ `my-web-net` ขึ้นมาเพื่อให้ Container ทั้ง 3 ตัวคุยกันได้

-   **Isolation:** แยกวงออกจาก Container อื่นๆ ในเครื่องเพื่อความปลอดภัย
    
-   **Service Discovery:** Container สามารถเรียกหากันได้ด้วย **ชื่อ Service** เช่น ในไฟล์ PHP เราใช้ `mysqli("db", ...)` ได้เลย ไม่ต้องรู้ IP Address
    

----------

## 🛠 5. รวมคำสั่งที่มีประโยชน์ (Cheat Sheet)

**คำสั่ง**

**ความหมาย**

`docker-compose up -d`

สร้างและเริ่มระบบ (ทำงานเบื้องหลัง)

`docker-compose down`

หยุดและลบ Container/Network ทั้งหมด

`docker-compose ps`

ดูสถานะว่ามีอะไรทำงานอยู่บ้าง

`docker-compose logs -f`

ดู Logs แบบ Real-time (กด Ctrl+C เพื่อออก)

`docker restart my-web-server`

รีสตาร์ทเฉพาะ Web Server

`docker exec -it my-mariadb bash`

เข้าไปใน Container Database (เช่น เพื่อพิมพ์คำสั่ง mysql)
